server:
  port: 8081
management:
#  endpoint:
#    shutdown:
#      enabled: true
#    info:
#      enabled: true
  endpoints:
    web:
      exposure:
        include: "*"
spring:
  application:
    name: monitoring
#        include: health, info, prometheus